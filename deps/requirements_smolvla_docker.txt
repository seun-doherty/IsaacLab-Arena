# Pinned versions from working env (deps/requirements_env_isaaclab.txt and env_isaaclab_dependencies.md).
# Do NOT add torch, torchvision, or torchaudio here; they are constrained in the Dockerfile to use Isaac Sim's bundle.
numpy==1.26.0
num2words==0.5.14
transformers==4.57.6
accelerate==1.12.0
diffusers==0.35.2
datasets==4.1.1
huggingface-hub==0.35.3
safetensors==0.7.0
# packaging: do not pin; lerobot requires packaging>=24.2,<26.0
av==15.1.0
draccus==0.10.0
einops==0.8.2
gymnasium==1.2.1
# Do not add imageio: Isaac Sim base image already has imageio 2.37.0 in pip_prebundle; let it satisfy lerobot.
jsonlines==4.0.0
deepdiff>=7.0.1,<9.0.0
pyserial>=3.5
# lerobot[smolvla] is installed in a separate Dockerfile RUN (after this) to avoid resolver conflicts with prebundled imageio.
